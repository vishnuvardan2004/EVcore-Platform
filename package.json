{
  "name": "evcore-platform-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd EVcore && npm ci --legacy-peer-deps && npm run build",
    "dev": "cd EVcore && npm run dev"
  },
  "workspaces": [
    "EVcore",
    "evcore-backend"
  ]
}
